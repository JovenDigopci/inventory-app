# Inventory App

This application is for managing inventory for a decant perfume shop.

The project requirements are documented in the `requirements/` folder. The approved MVP stack is:

- Frontend: HTML, CSS, JavaScript
- Backend: Node.js with Express
- Database: MySQL Community Server

Implementation files are in `src/`, database migrations are in `database/`, and setup instructions are in `SETUP.md`.
